@spec childclass-nested
  @options
    @groupByApi false
    @reverseVisibleList false
  @input
    @changelogList
      @process
        @api ApiName
          @childclass test1
            @childclass test2
              @childclass test3
                @added 0.1.0
                  @description: Child class added
      @version 0.1.0
  @output
    @changelogList
      @changelog 0.1.0
        @entry
          @header css
            @childclass test1
              @childclass test2
                @childclass test3
          @change added
            @description: Child class added
