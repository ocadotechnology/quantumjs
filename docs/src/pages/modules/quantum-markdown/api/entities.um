@entity @markdown
  @added 2.0.0
  @description
    A renderer for rendering @code[markdown] using quantum

  @extra
    @example
      @@um
        @@markdown
          ### Heading

          _italic_ or **bold** text.

@entity markdown
  @added 2.0.0
  @description
    A renderer for including markdown files in quantum.

  @extra
    @example
      @noOutput
      @@um
        @markdown: @inline path/to/file.md
