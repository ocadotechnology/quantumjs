@entity markdown
  @added 2.0.0
  @description
    A renderer for rendering @code[markdown] using quantum

  @extra
    @example
      @@um
        @@markdown
          ### heading

          _italic_ or **bold** text.

    @code(um)[@inline] can also be used:
    @@codeblock um
      @markdown: @inline path/to/file.md
